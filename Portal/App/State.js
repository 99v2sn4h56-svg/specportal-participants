/**
 * Spec Central full-page app state boundary.
 *
 * Apps Script `.js` files execute server-side, so the browser runtime state is
 * currently implemented inside SpecCentral.html as `window.App.state`.
 *
 * Keep this file as the source map for the future client bundling/extraction
 * pass. Do not add DOM code here until the frontend has a build/include path
 * for client JavaScript modules.
 */
