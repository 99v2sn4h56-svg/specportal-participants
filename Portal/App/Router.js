/**
 * Spec Central full-page router boundary.
 *
 * Current browser implementation:
 *   App.navigate("dashboard")
 *   App.navigate("participants")
 *   App.navigate("attendance")
 *
 * The live client router is embedded in SpecCentral.html so the Apps Script
 * web app can run without a bundler. Extract it here only when this file can
 * be included as client-side JavaScript rather than server-side Apps Script.
 */
